'use babel';

import MinimalIconsMenuTollsView from '../lib/minimal-icons-menu-tolls-view';

describe('MinimalIconsMenuTollsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
